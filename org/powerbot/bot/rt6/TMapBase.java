package org.powerbot.bot.rt6;

import java.awt.Graphics;
import org.powerbot.bot.DebugHelper;
import org.powerbot.script.TextPaintListener;
import org.powerbot.script.Tile;
import org.powerbot.script.rt6.ClientAccessor;
import org.powerbot.script.rt6.ClientContext;
import org.powerbot.script.rt6.Game;

public class TMapBase extends ClientAccessor implements TextPaintListener
{
  public TMapBase(ClientContext a)
  {
    a.<init>(a);
  }
  
  public int draw(int a, Graphics a) {
    Tile localTile = ctx).game.mapOffset();
    if (localTile != null) { tmpTernaryOp = localTile.toString(); break label49; throw (a++).insert(0, NodeQueue.jdMethod_this("Q`l!~`od&!")); } label49: DebugHelper.drawLine(a, a, "");
    return a;
  }
}
